import { DefaultTheme } from "styled-components";

const theme: DefaultTheme = {
  colors: {
    primary: "#ffffff",
    success: "#00ff00",
  },
  zIndex: {
    background: 1,
    land: 10,
    player: 100,
    foreground: 1000,
    ui: 10000,
  },
};

export default theme;
